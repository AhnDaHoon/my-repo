name: Change shell
on: push
jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - shell: bash          # 셸을 Bash로 지정
        run: echo "${SHELL}"
      - shell: python        # 셸을 Python으로 지정
        run: |
          import os
          print(os.environ['PATH'])
